[project]
name = "chat-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Eglibra", email = "garoubieu@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
chat-project = "chat_project:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
